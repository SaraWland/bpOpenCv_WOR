#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class ShapeType {
    CIRCLE,
    RECTANGLE,
    TRIANGLE,
    SQUARE,
    HALF_CIRCLE,
    UNKNOWN,
    EXIT
};

enum class Color {
    PINK,
    GREEN,
    YELLOW,
    ORANGE,
    UNKNOWN
};

#endif